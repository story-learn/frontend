import { FC } from "react";
import { Navbar } from "../../components";

const Index: FC = ({ children }) => {
    return (
        <>
            {/* <Navbar /> */}
            {children}
        </>
    );
};

export default Index;
