import { FC } from "react";

const UploadInstruction: FC = () => {
    return (
        <p className="upload__header-para">
            Click the pen icon to upload text. Click the camera icon to upload
            image. Check the preview section to preview the frames. Click upload
            to upload.
        </p>
    );
};

export default UploadInstruction;
